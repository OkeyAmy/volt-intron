export default function Home() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1>Sautice</h1>
      <p>Voice gateway is live at <code>/api/voice/stream</code>. UI in progress.</p>
    </main>
  );
}
