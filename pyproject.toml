[project]
name = "sautice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "harystyleseze", email = "harystyleseze@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "numpy>=2.5.3",
    "soundfile>=0.14.0",
    "websockets>=17.1",
]

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
